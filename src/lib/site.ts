export const siteConfig = {
  name: 'BalanceBuddy',
  description:
    'A shared expense app for groups to track expenses, balances, and settle-ups in one place.',
} as const

export const defaultMetaTitle = `${siteConfig.name} | Shared Group Expenses`
